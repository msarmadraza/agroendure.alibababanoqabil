(async () => { const key = process.env.EXPO_PUBLIC_GEMINI_API_KEY || process.env.GEMINI_API_KEY || ''; try { const r = await fetch('https://generativelanguage.googleapis.com/v1beta/models?key=' + key); const j = await r.json(); const names = (j.models || []).map(m => m.name.replace('models/', '')).filter(n => n.includes('flash')); console.log('STATUS:', r.status); console.log('FLASH_MODELS:', names.join(', ')); } catch (e) { console.log('ERR', e.message); } })();
