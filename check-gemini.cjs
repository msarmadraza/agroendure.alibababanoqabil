(async () => { const key = process.env.EXPO_PUBLIC_GEMINI_API_KEY || process.env.GEMINI_API_KEY || ''; try { const r = await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=' + key, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ contents: [{ parts: [{ text: 'Reply with only: OK' }] }] }) }); const j = await r.text(); console.log('STATUS:', r.status); console.log('BODY:', j.substring(0, 400)); } catch (e) { console.log('ERR', e.message); } })();
